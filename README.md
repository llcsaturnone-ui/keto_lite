# Простой трекер питания v10

Что добавлено:
- Вместо длинной вкладки `Ритм` появились быстрые вкладки снизу: `Дела`, `Намаз`, `Трен.` и `База`.
- Шаги хранятся по дням, цель шагов можно менять.
- Напоминания можно отмечать, добавлять, редактировать, удалять и перетаскивать выше или ниже.
- Незавершенные дела переходят на следующий день автоматически, сделанные не переносятся; уже открытый будущий день тоже пересверяется с предыдущим.
- У каждого намаза теперь две отметки: намаз и зикр (суохьаж).
- Во вкладке `Намаз` появился большой счетчик зикра с отметками 33 / 66 / 99 и сбросом удержанием 5 секунд; он расположен под списком намазов.
- Тренировки стали проще: отжимания, приседания, подтягивания, планка и пресс; у каждого упражнения есть повторения в подходе, три галочки подходов и общий счётчик.
- Повторения в тренировках сохраняются как значение на завтра, чтобы не вводить их каждый день.
- Backup теперь находится в настройках по шестерёнке сверху и включает питание, продукты, цели, дела, намазы, тренировки и шаги.

Что улучшено:
- Отрицательные граммы, КБЖУ и значения продуктов больше не попадают в расчёты.
- Backup обновлён до версии 10, старые backup v7/v8/v9 остаются совместимыми.
- В шапке приложения появилась отдельная метрика для каждой основной вкладки.
- Кнопки с иконками получили доступные подписи для браузера и скринридеров.
- Вкладки `Дневник` и `База` визуально приведены к стилю вкладки `Ритм`.
- Нижняя навигация закреплена к низу окна, а iPhone Safari больше не должен оставлять пустоту снизу после открытия и закрытия клавиатуры.
- Поля ввода на iPhone поднимаются над клавиатурой, чтобы было видно, что редактируется.
- В карточках белков, жиров и углеводов теперь видно, сколько осталось или где перебор.
- Последний добавленный или обновлённый продукт поднимается наверх списка.
- В дневнике съеденные продукты показываются от новых к старым.
- При удалении записи из блока `Съедено` приложение спрашивает подтверждение.
- Выполненные напоминания автоматически уходят вниз, а новые появляются сверху.
- Перетаскивание напоминаний стало живым и стабильнее: карточка идёт за пальцем, соседние карточки перестраиваются.
- Карточки тренировок стали компактнее: меньше пустот, ввод повторений, подходы и итог в одной строке.
- Кнопки подходов в тренировках стали крупнее.

Что стоит улучшить дальше:
- Для продакшена лучше собрать React/Tailwind локально, а не грузить их через CDN и Babel прямо в браузере.

Формат для массового добавления продуктов:
```json
[
  {"name":"Мандарин","calories":53,"protein":0.8,"fat":0.3,"carbs":13.3},
  {"name":"Киви","calories":61,"protein":1.1,"fat":0.5,"carbs":14.7}
]
```

Открой `index.html` в браузере.


## Иконка приложения

Добавлены файлы:
- `icon-180.png` — иконка для iPhone / iOS.
- `icon-192.png` — иконка для Android/PWA.
- `icon-512.png` — крупная иконка для Android/PWA.
- `manifest.webmanifest` — manifest для PWA.

В `index.html` добавлены только строки в `<head>` для подключения иконок. Логика приложения не менялась.

Если хочешь свою иконку, просто замени эти PNG-файлы на свои с такими же именами.
На iPhone после замены иконки нужно удалить старую иконку с экрана «Домой» и заново добавить сайт через Safari → Поделиться → На экран «Домой».
